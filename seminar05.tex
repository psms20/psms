\documentclass[a4paper, 12pt]{article}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% autocompile publish

\usepackage{math-hse}
\title{Совместное распределение дискретных случайных величин.}
\renewcommand{\thesubsection}{\arabic{subsection}}
\newcommand{\E}{$\text{E}$}
\newcommand{\D}{$\text{D}$}
\newcommand{\Cov}{$\text{Cov}$}
\newcommand{\Corr}{$\text{Corr}$}

\date{14.02.2020}
\begin{document}

\begin{problem}
Совместное распределение двух случайных величин $X$ и $Y$ 
задано таблицей (с пропущенной вероятностью):
\begin{table}[ht!]
\centering
\begin{tabular}{|c|c|c|}
\hline
X \textbackslash Y & $0$  & $2$   \\ \hline
$0$                 & $0.4$ & $?$  \\ \hline
$3$                  & $0.1$ & $0.3$  \\ \hline
\end{tabular}
\end{table}

\begin{enumerate}
\item Выпишите маргинальные распределения случайных величин $X$ и $Y$.
\item Проверьте, являются ли случайные величины $X$ и $Y$ независимыми.
\item Запишите закон распределения $X\cdot Y$.
\item Найдите $\E(X)$, $\E(Y)$, $\D(X)$, $\D(Y)$.
\item Найдите $\Cov(X,Y)$, $\Corr(X,Y)$.
\end{enumerate}
Проинтерпретируйте полученные значения ковариации и 
корреляции: укажите направление связи между 
случайными величинами и силу связи.
\end{problem}

\begin{problem}
Совместное распределение двух случайных величин 
$X$ (выигрыш от игры) и $Y$ (число взятых подсказок) задано таблицей:
\begin{table}[ht!]
\centering
\begin{tabular}{|c|c|c|c|}
\hline
X\textbackslash Y & $-10$  & $0$  &  $10$  \\ \hline
$0$                & 0.1 & 0.1  & 0.3  \\ \hline
$1$                & 0.1 & 0.2 & 0.2   \\ \hline
\end{tabular}
\end{table}

\begin{enumerate}
\item Выпишите маргинальные распределения вероятностей 
случайных величин $X$ и $Y$. Являются ли случайные 
величины $X$ и $Y$ независимыми?
\item Запишите законы распределения случайных 
величин $X+Y$ и $X\cdot Y$.
\item Вычислите корреляцию между $X$ и $Y$ и 
проинтерпретируйте полученный результат.
\end{enumerate}
\end{problem}

\begin{problem}
Известно, что $\D(X) = 4$, $\D(Y) = 9$, $\Cov(X,Y) = -2$. 
Найдите корреляцию между случайными величинами $X$ и $Y$. 
Проинтерпретируйте полученный результат. 
\end{problem}

\begin{problem}
Известно, что $\D(X)=2$, $\D(Y)=1$, Случайные величины 
$X$ и $Y$ не являются независимыми, и известно, что $\Cov(X,Y)=0.5$. 
Найдите дисперсию случайной величины $W$: 
a) $W=X+Y$; b) $W=-3X+2Y + 5$.
\end{problem}

\begin{problem}
$X$ и $Y$ -- дискретные случайные величины. 
Известно, что $\Cov(X,Y)=4$. Найдите ковариацию 
случайных величин $V$ и $W$: 
a) $V=2+3X$, $W=5-7Y$; b) $V=5-X$, $W=12+6Y$.
\end{problem}

\end{document}
